include ../point-bar/point-bar

mixin skills(skills)
    .skills
        .skills__header
            +header(skills.icon, skills.header)
        .skills__content
            each skill in skills.skillsList
                .skills__list
                    +point-bar(skill)
