
mixin contact(contact)
    .contact
        .contact__header
            +header(contact.icon, contact.header)
        .contact__content
            each cont in contact.contacts
                .contact__row
                    .contact__name= cont.name
                    .contact__value= cont.value