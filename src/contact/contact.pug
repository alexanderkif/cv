
mixin contact({icon, header, contacts})
    .contact
        .contact__header
            +header(icon, header)
        .contact__content
            each cont in contacts
                dl.contact__row
                    dd.contact__name= cont.name
                    dt.contact__value= cont.value